// Ejemplo AND
let idad = 25;
let tieneLicencia = true;

// si la edad es mayor de 18 y tiene una licencia...
if (idad > 18 && tieneLicencia) {
  console.log("Puede conducir!");
} else {
  console.log("No puede conducir!");
}

// Ejemplo OR

// si tiene manzana o tiene banama…
if (tieneManzana|| tieneBanana) {
    console.log("Tienes frutas!");
  } else {
    console.log("No tienes frutas.");
  }

/*
// Operadores de comparación

> // Mayor
< // Menor
>= // Mayor o igual
<= // Menor o igual
== // Igual a (comparación)
=== // Comparación escrita (Sí solo sí es igual)
!= // Diferente de

// Operadores lógicos
&& // AND (Se cumple si ambos son verdaderos)
|| // OR (Se cumple si uno es vedadero
! //  Not (Niega la comparacion)

// Operadores booleanos
True
False

*/